{
  "name": "Online-Auction-WebApp",
  "scripts": {
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "setup": "npm install --prefix server && npm install --prefix client",
    "ic": "npm install --prefix client --",
    "sc": "npm install --prefix server --"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
